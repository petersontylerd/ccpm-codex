# schema_version: 1.0.0
metadata:
  feature_id: "F001"                   # e.g., "F001" (sequential within epic)
  parent_epic: "E004"                  # e.g., "E001" (must match parent epic ID)
  feature_name: "Agent Scheduler"
  date: "2025-09-19 15:04:16 CT"
  facilitator: "Codex PM"
  based_on_artifacts:
    - "brainstorm.yaml"
    - "vision.yaml"
    - "strategy.yaml"
    - "roadmap.yaml"
    - "development-considerations.yaml"
    - "prd.yaml"
    - "personas.yaml"
    - "metrics.yaml"
  schema_version: "1.0.0"

overview:
  description: "Coordinate agent scheduling and load balancing"                  # 2–4 sentence summary of functionality/value
  user_value: "Ensures cross-agent throughput stays high"                   # why users care
  in_scope: []                     # [string] functionality included
  out_of_scope: []                 # [string] functionality explicitly excluded
  personas_served: []              # persona IDs
  workflows_addressed: []          # workflow IDs
  linked_prd_frs: []               # FR IDs
  linked_strategy_goals: []        # SG IDs
  linked_roadmap_horizons: []      # short-term | mid-term | long-term
  linked_metrics: []               # metric IDs (M-01, NS-01, etc.)
  dev_considerations_notes: ""     # feasibility/system notes from dev-considerations

dependencies:
  internal_features: []            # other feature IDs
  external_dependencies: []        # vendor/API/platform dependencies

success_criteria:
  - id: ""                         # e.g., "FSC01"
    metric: ""                     # reference metric ID
    target: ""                     # measurable success signal
    notes: ""

risks_assumptions:
  - id: ""
    description: ""
    type: ""                       # tech | data | ops | adoption
    mitigation: ""
    owner: ""

prioritization:
  priority: "Must"                     # Must | Should | Could | Won't
  rationale: ""